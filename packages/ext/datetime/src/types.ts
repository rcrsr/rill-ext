/**
 * Type definitions for datetime extension.
 *
 * @module
 */

/** Datetime extension configuration. */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- Intentionally empty; fields added in future versions
export interface DatetimeExtensionConfig {}
