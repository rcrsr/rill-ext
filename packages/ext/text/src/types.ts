/**
 * Type definitions for text extension.
 *
 * @module
 */

/** Text extension configuration. */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface TextExtensionConfig {
  // No fields — text extension requires no configuration.
}
